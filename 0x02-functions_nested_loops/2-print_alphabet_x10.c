#include <stdio.h>

/**
 * main - entry
 *Description: writing function that prints 10 times the alphabet.
 *Return: (0) success
 */

int main(void)
{
	int alph, o;

	alph = 0;

	while (alph < 10)
	{
		alph++
			for (o = 'a' ; o <= 'z' ; o++)
			{
				_putchar(o);
			}
		o++
		_putchar('\n');
	}
}
