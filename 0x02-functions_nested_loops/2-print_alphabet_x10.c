#include <stdio.h>

/**
 * main - entry
 *Description: writing function that prints 10 times the alphabet.
 *Return: (0) success
 */

int main(void)
{
	int alph, or;

	alph = 0;

	while (alph < 10)
	{
		alph++
			for (or = 'a' ; or <= 'z' ; or++)
			{
				_putchar(or);
			}
		or++
		_putchar('\n');
	}
}
